node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example

# Heavy source media — kept locally, never committed.
# Videos: ~700 MB of LPGA footage. Source photos: ~60 MB of camera originals.
# The delivery set lives in apps/holobox/public/assets/photos and IS committed.
# Regenerate the delivery set with: python3 scripts/optimize-photos.py
projects/*/Videos/
projects/*/source-photos/
